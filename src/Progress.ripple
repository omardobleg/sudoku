export component Progress({ progress }: { progress: number }) {
	<div class='m-3 mx-auto max-w-3xl bg-indigo-50 rounded-full h-2.5 dark:bg-gray-700'><div class='bg-indigo-500 h-2.5 rounded-full' style={`width:${progress}%`} /></div>
}